app:
  base-url: https://scdev4--recipit-api--qj5x98zdln7d.code.run

cors:
  allowed-origins:
    http://localhost:8081,
    https://scdev4--recipit-mo--m2p477ffsl99.code.run,
    https://mockiwi.com

spring:
  datasource:
    hikari:
      minimum-idle: 3
      maximum-pool-size: 5

server:
  servlet:
    session:
      cookie:
        same-site: Lax
        domain: .mockiwi.com